<% games.each do |game| %>
  <a href="/games/<%= game['id'] %>"><%= game['name'] %></a>
  <% if admin? %>
    <a href=<%= "/admin/games/#{game['id']}/edit" %>>edit</a>
    <form action="/admin/games/<%= game['id'] %>/delete" method="post">
      <input type="submit" value="Delete game">
    </form>
  <% end %>
<% end %>
<%= erb :'tags/index' if admin? %>
